---
import Layout from '../../layouts/Layout.astro';
import LocaleHomeStub from '../../components/LocaleHomeStub.astro';
import { deHome } from '../../i18n/stubs/de';
import { ml, siteStructuredDataIds } from '../../consts';

export const prerender = true;

const locale = 'de';
const canonicalPath = '/de/';

const origin = Astro.site?.origin ?? 'https://oopbuyai.com';
const { websiteId } = siteStructuredDataIds(origin);
const pageUrl = new URL(canonicalPath, Astro.site).href;

const webPageLd = {
  '@context': 'https://schema.org',
  '@type': 'WebPage',
  '@id': `${pageUrl}#webpage`,
  name: deHome.webPageName,
  url: pageUrl,
  description: deHome.description,
  inLanguage: 'de',
  isPartOf: { '@id': websiteId },
  keywords: deHome.webPageKeywords,
};
---

<Layout
  locale={locale}
  title={deHome.title}
  description={deHome.description}
  keywords={deHome.keywords}
  canonicalPath={canonicalPath}
  i18nRouteSegment=""
  jsonLd={webPageLd}
>
  <LocaleHomeStub content={deHome} />
</Layout>
